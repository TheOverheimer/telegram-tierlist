# Использование Gunicorn для продакшена

Для запуска с автосбором участников на сервере используйте:

```bash
gunicorn app:app --bind 0.0.0.0:$PORT --worker-class sync --workers 1 --threads 2
```

Автосбор будет запускаться автоматически в фоновом потоке при старте приложения.
